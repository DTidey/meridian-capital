# Core data
yfinance>=0.2.40
sqlalchemy>=2.0
psycopg2-binary>=2.9
lxml>=5.0.0

# Testing
pytest>=7.0
pytest-mock>=3.0
responses>=0.25
pandas>=2.0.0
requests>=2.31.0

# Config + env
pyyaml>=6.0
python-dotenv>=1.0.0

# Layer 3 — AI analysis
openai>=1.30.0
tiktoken>=0.7.0

# Layer 4 — Portfolio construction
scipy>=1.13.0

# Layer 5 — Risk management (parquet cache for predicted covariance + stress data)
pyarrow>=14.0.0

# Layer 6 — Execution (Alpaca)
alpaca-py>=0.21.0

# Layer 7 — Reporting & Dashboard
streamlit>=1.35.0
plotly>=5.22.0
matplotlib>=3.8.0
pytz>=2024.1

# Optional — install if you have the API keys
# polygon-api-client>=1.12.0   # Polygon prices
# fredapi>=0.5.0               # FRED macro data
